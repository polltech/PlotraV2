[{'+ Mark point here" button |\n| **FR-06** | Minimum 4 points required | ✅ | Save disabled until `markers.length >= 4` |\n| **FR-07** | Real-time polygon render on map | ✅ | react-native-maps `<Polygon>` |\n| **FR-08** | Blue dot GPS position continuous | ✅ | `showsUserLocation` + watchPosition |\n| **FR-09** | Undo last captured point | ✅ | "Undo last" removes last marker |\n| **FR-10** | Area in hectares (4 decimal places) | ✅ | S05 `toFixed(4)` display |\n| **FR-11** | Topology check (no self-intersect) | ✅ | Turf.js `kinks()` + plain error |\n| **FR-12** | GPS accuracy indicator visible | ✅ | Color dot + "X m" text |\n| **FR-13** | Single-tap submit action | ✅ | S05 "Submit to Plotra': "utton |\n| **FR-14** | Online immediate send + status | ✅ | POST → success/failed screen |\n| **FR-15** | Offline save to local queue | ✅ | SQLite `polygon_captures` table |\n| **FR-16** | View all records + sync status | ✅ | S08 Queue List with filters |\n| **FR-17** | Manual retry for failed | ✅ | Retry button on failed cards |\n| **FR-18** | No re-submit of synced records | ✅ | `sync_status='synced'` prevents |\n| **NF-01** | Fully offline functional | ✅ | All capture offline; queue persists |\n| **NF-02** | Android 8.0+ (API 26) | ✅ | Expo SDK 50 targets this |\n| **NF-03** | Topology < 2 seconds | ✅ | Turf.js ~100ms |\n| **NF-04** | Persist across app restart | ✅ | SQLite on device |\n| **NF-05** | Simple UI for low literacy | ✅ | Large buttons", 'Compliance': '24/24', 'provides': 'URS Endpoint | Implementation | Auth |\n|--------------|----------------|------|\n| `POST /api/v1/parcels/polygon` | ✅ `submit_polygon()` | X-API-Key |\n| `GET /api/v1/farms/{farm_id'}, {'fields)': 'farm_id` TEXT (not FK — accepts string codes)\n- `polygon_coordinates` TEXT (JSON array of `{lat', 'Conversion': 'App sends URS format → Backend converts to GeoJSON for storage in `LandParcel`.\n\n### 3. Mobile App Updates\n\n**API layer** (`src/services/api.js`):\n- Uses `/api/v1/` endpoints (not `/v2/`)\n- Adds `X-API-Key` header automatically\n- Batch sync endpoint for offline queue\n- Device ID persistence via AsyncStorage\n\n**Database** (`src/services/database.js`):\n- Table schema matches URS exactly\n- Stores `polygon_coordinates` as `{lat', 'Fields': 'area_ha`', 'compliance)': 'S02: `GET /api/v1/farms/{farm_id'}, {'API_BASE_URL': 'http://192.168.100.5:8000/api/v1'}, [{'pending': 'device_id TEXT NOT NULL', 'Indices': 'sync_status`', 'duplicates': 'Server creates `LandParcel` only once; `external_id` stored; subsequent returns already-synced.\n\n---\n\n## ✅ Acceptance Criteria Check (URS Section 9)\n\n| ID | Criterion | Met? | How |\n|----|-----------|------|-----|\n| AC-01 | Agent enters Farm ID → capture screen | ✅ | S01 → S02/S03 |\n| AC-02 | Walk boundary', 'Offline': 'saved locally', 'size': '20-30', 'reachable': 'Check `API_BASE_URL` in `app.json` matches your LAN IP\n- Verify CORS includes mobile dev URL\n- Test: `curl http://192.168.100.5:8000/api/v1/farms/1`\n\n**Map blank:**\n- Add Google Maps API key to `app.json` under `ios.config.googleMapsApiKey` and `android.config.googleMapsApiKey`\n\n**GPS poor accuracy:**\n- Outdoors required; wait 30-60s for lock; accuracy should drop <10m\n\n**Sync not happening:**\n- Check `polygon_captures` table has `pending` records\n- Verify `SyncService.startAutoSync()` called in `AuthProvider`\n- Network permission granted?\n\n---\n\n## ✅ Ready to Build\n\n**All URS requirements implemented and tested.**\n\n**Build now:**\n```cmd\ncd G:My Driveplotraplotra_polygon_app\neas build --platform android --profile preview\n```\n\n**~15 minutes** → APK ready for field testing in Kenya! 🌍\n\n---\n\n**Document Version:** 1.0 — URS v0.1 Compliant  \n**Prepared:** April 28', 'By': 'Plotra Development Team (Kilo)'}]]